[project]
name = "mapping-for-drone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "uv>=0.5.15",
    "httpx[http2]>=0.28.1",
    "python-multipart>=0.0.21",
    "websockets>=14.0",
    "fastapi>=0.136.1",
]
